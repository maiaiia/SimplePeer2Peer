# SimplePeer2Peer

A basic multithreaded peer-to-peer file-sharing network written in python in order to practice socket programming.

## Task
Build a server that facilitates a peer-to-peer (P2P) file-sharing network. The server maintains a directory of files and connected peers. Clients can request files, and the server connects them to peers that have the requested files. Data transfers happens over TCP.
